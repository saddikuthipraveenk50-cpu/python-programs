def pascal(n):
    triangle = []
    for i in range(n):
        row = [1]
        if triangle:
            last = triangle[-1]
            row += [sum(pair) for pair in zip(last, last[1:])]
            row.append(1)
        triangle.append(row)
    return triangle

for row in pascal(5):
    print(row)
